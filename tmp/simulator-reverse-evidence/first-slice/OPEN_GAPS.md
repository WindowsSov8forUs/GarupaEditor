# 第一切片证据缺口

本文件只记录会影响第一切片框架真实性、但在锁定证据提交中尚未闭合的边界。未闭合项不得由实现默认值、旧模拟器行为或常见音游做法替代。

## G01：`UnitsPerBar` 名称与数值

- 状态：`evidence-required`
- 已确认：`InGameMusicScoreController` 使用 `beatProgress + UnitsPerBar * barProgress`，并以 BPM 推进进度。
- 未确认：静态字段的原始托管名称和数值。
- 来源：E03。
- 实现约束：需要数值的夹具必须携带独立证据引用；生产构造器不得设置默认值。

## G02：相同位置成员的上游构造顺序

- 状态：`evidence-required`
- 已确认：音符组依成员现有顺序激活，活跃列表随后反向更新。
- 未确认：BMS 转换阶段对相同位置成员采用 lane、源顺序或其他稳定键。
- 来源：E04、E10。
- 实现约束：第一切片夹具必须显式提供源顺序，不得重新排序。

## G03：跨 Note 低索引移除实例

- 状态：`evidence-required`
- 已确认：反向遍历使用固定递减索引，活跃列表突变在同一子步可见。
- 未确认：具体已表示 Note 在 Update 中移除另一个低索引 Note 的原作调用路径。
- 来源：E07。
- 实现约束：只复原已确认的数据结构语义，不制造跨 Note 移除行为。

## G04：Unity PlayerLoop 精确相位

- 状态：`evidence-required`
- 已确认：`NoteManager.ExecUpdate` 内部的音乐推进、Update、AfterUpdate 和音符组处理顺序。
- 未确认：该方法相对其他 MonoBehaviour、渲染、音频和原生 PlayerLoop 阶段的位置。
- 来源：E03、E05、E07。
- 实现约束：宿主 `step` 是可移植驱动边界，不得宣称等同 Unity PlayerLoop。

## G05：暂停状态的精确更新门条件

- 状态：`evidence-required`
- 已确认：暂停/恢复状态写入、音频/控制广播顺序，以及暂停不修改时钟、BPM、组游标或对象池。
- 未确认：暂停状态 `1/2` 和游戏状态 `5/7` 的完整枚举名称及精确帧循环门条件。
- 来源：E09。
- 实现约束：第一切片只冻结宿主调度，不命名原作枚举或复原暂停 UI。

## G06：慢帧历史计数器规则

- 状态：`evidence-required`
- 已确认：帧 delta 的 1–4 子步阈值，以及历史计数器可将调度强制回单步。
- 未确认：所有计数器的用户态名称、完整累积和重置规则。
- 来源：E03。
- 实现约束：先实现确定的阈值选择；历史强制规则必须保留证据门，不得猜测。
