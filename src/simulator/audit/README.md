# Simulator tracked audits

Tracked audit files document release claims and provenance only. Production code does not import this directory. `layout-scalar-provenance.json` records the adaptive-layout source classes and zero-count gates; executable verification remains in `testing/verifyAdaptiveLayoutStatic.mjs` and the manifested Reverse fixture.
