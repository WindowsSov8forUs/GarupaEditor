# 项目工作约定

用户当前明确指令优先；仅执行已授权范围，不因发现相邻问题自动扩大任务。

## 开始与续接

- 先读本文件、存在的本地`CLAUDE.md`及其中指定的当前任务摘要；随后按需定位代码与合同，不默认通读全部历史。
- Simulator原作算法工作遵守[证据工作流](src/simulator/evidence-workflow.md)。稳定规范不追记提交历史，宿主路径和当前进度只放ignored记录。
- 修改前检查当前分支和工作树，保护用户已有修改。用当前摘要及精确源码检索续接，不重新开展已完成的调查。

## 实际目标与验收边界

- Simulator实现不应将代码用于铺平平台差异。使用宿主数学、字体、图形与音频能力；不模拟ARM指令、Unity内部缓存/内存布局或驱动行为来追求逐位、逐像素一致。必要的坐标、单位与资源格式转换属于接口适配，不属于平台仿真。
- Simulator“明确无算法差异”指当前已承载功能的计算、状态变化和生产输出语义对齐原作，不要求用Tauri/Pixi复刻Unity引擎。
- 具体功能范围、平台适配、问题分类与完成条件以[算法对齐规范](src/simulator/evidence-workflow.md)为准；不得以内部配置不同直接认定算法错误，也不得把必要的算法问题当平台差异排除。
- 已确认差异、待验证候选和必要证据缺口分开汇报。引擎内部细节只有影响范围内行为时才调查；不以补齐缓存、对象身份或全引擎覆盖作为完成条件。

## 范围与资源

- 每阶段明确具体问题、生产消费者、必要依赖和验收条件；区分已确认差异、证据缺口、范围内已一致。非必要的新问题暂留工作上下文，不自动展开。
- 每60分钟复核范围与产出。无有效进展时换方法或说明阻塞，不机械重复；仅对确需用户决策的范围变化提问。
- 默认一个重型进程、短输出；轻量独立只读可合并。未经用户或适用规范明确要求，不启动子代理。
- 大文件先查结构和指定字段；成功只输出结论与计数，失败只输出首批关键差异。完整JSON、长反编译结果和历史日志不灌入会话。
- 优先复用工具；不为记录状态或重复检查另建框架。稳定规范、当前状态、历史证据分别维护，避免重复同一段内容。

## 文档更新

- 仅在任务有明显进展时编写或更新文档，包括tracked文档、ignored任务摘要和临时说明。明显进展指具体问题完成修复及相关验证、证据实质改变结论或必要行动，或用户明确调整要求与范围。
- 阅读、检索、工具调用或重试、零散修改、单独编译通过、时间流逝及回合结束均不构成文档更新理由。没有明显进展时只在对话中简述状态，不追加日志或重写摘要。
- 同一阶段的明显进展合并为一次最小文档更新，只写入承担该信息的文档；不在规范、合同、任务摘要和验收摘要中重复记录。范围外发现待此时按必要性汇总，不逐项即时落文档。
- 必要的工具原始输出可由现有工具保存供验证复用，但不因此另写叙述报告，不将输出文件数量视作进展。稳定规范仅随实际规则变化更新。

## 验证与交付

- 验证随改动及其依赖确定；相同源码、输入和工具版本的结果可复用。相关依赖变化或覆盖不明时必须重验。
- 文档改动检查内容、链接及仓库政策，不运行无关编译、应用或测试。新增、修改或生成测试须有用户当前明确授权。
- 编译通过、提交数或局部对照不能证明整体算法等价；不得把候选或历史完成声明升级为当前完整闭合。
- 一个commit一个意图，一个PR一个问题；重构与行为修复分开，相关文档和验证归入同一问题，无需按工具调用拆提交。
- commit采用`type(scope): 中文描述`，PR标题语义标签加中文、正文中文。只使用当前Git账户署名，不加生成标记。
- 不新建、不切换分支，冻结分支不得修改。精确暂存并检查差异；按当前交付要求推送。Reverse证据必须先验证、提交、推送并确认远端同步，才允许生产消费。

Windows文件操作用明确路径，不跨shell拼接删除，不把`NUL`作为项目相对重定向目标。任务收尾做一次排除`.git`和junction的NUL检查；发现时验证目标范围后删除并复查。保留用户文件、依赖、release必需DLL及tracked平台模板。ignored资料不强制加入Git，宿主绝对路径不写入tracked文件。

二进制逆向优先`ida-pro-mcp`；仅确实不可用时说明原因再回退。修改IDA数据库前说明具体修改，临时数据库状态不能替代登记证据。
